{
  "name": "attn",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "start": "bun install --no-summary && bun packages/plugin/index.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
