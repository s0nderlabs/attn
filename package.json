{
  "name": "attn",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "start": "bun install --no-summary || (rm -rf node_modules packages/*/node_modules && bun install --no-summary --backend=copyfile) && cd packages/plugin && bun index.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
