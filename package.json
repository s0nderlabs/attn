{
  "name": "attn",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "start": "bun bootstrap.ts && cd packages/plugin && bun index.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
