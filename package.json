{
  "name": "attn",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "start": "bun install --no-summary --backend=copyfile && bun packages/plugin/index.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
